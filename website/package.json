{
  "name": "osera-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npx --yes serve . -l 3000",
    "build": "rm -rf build && mkdir -p build && cp index.html guiding-principles.html press-release.html build/ && cp -r img build/"
  },
  "engines": {
    "node": ">=20.0"
  }
}
